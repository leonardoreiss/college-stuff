/*
Escreva uma função recursiva que recebe um valor v[b:e), b<=e, um int X, e um int Y
e substitui toda ocorrencia de X em v[b:e) por y
*/

#include <iostream>

using namespace std;

int recu(int v[], int x, int y, int cont)
{
    if()
}


int main()
{
    int v[]={2,1,4,10,5,1};
    int x = 2;
    int y = 10; 

    recu(v, x, y, 0);
}